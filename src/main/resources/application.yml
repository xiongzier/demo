spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dbsc0423?useSSL=false&serverTimezone=UTC
    username: root
    password: Lzh20030526.
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8080
